export enum DiscountType {
    QUANTITY = "QUANTITY",
    PRICE = "PRICE"
}

export enum DiscountValueType {
    PERCENTAGE = "PERCENTAGE",
    PRICE = "PRICE"
}

export enum Unit {
    EACH = "EACH",
    TOTAL = "TOTAL"
}